# Database Configuration
# For local PostgreSQL (Docker):
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/frontend_mcp

# For remote PostgreSQL (production - e.g., Supabase, Railway, Render):
# DATABASE_URL=postgresql://postgres:[PASSWORD]@host:5432/dbname

# API Key (local dev or production service key)
API_KEY=local-dev-key

# GitHub Configuration (optional, only for private repos)
GITHUB_TOKEN=

# Server Configuration
TEMP_DIR=/tmp/mcp-repos
PORT=8080

